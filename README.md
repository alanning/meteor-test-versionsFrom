meteor-test-versionsFrom
========================

A test app for Meteor 0.9 `api.versionsFrom`


Try different Meteor versions (`.meteor/release`) with various incantations in `packages/roles/package.js`.
